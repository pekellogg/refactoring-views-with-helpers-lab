<ul>
  <% @songs.each do |song| %>
    <li>
      <p><%= display_artist(song) %> – <%= link_to(song.title, song_path(song)) %></p>
    </li>
  <% end %>
</ul>

<br>

<%= link_to("Add New Song", new_song_path) %>
